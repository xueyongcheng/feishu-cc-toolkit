---
description: Show current session context usage (Feishu entry shortcut)
allowed-tools: Bash(~/.lark-channel/ctx.sh), Bash(sh ~/.lark-channel/ctx.sh)
---

Run `~/.lark-channel/ctx.sh` and relay its output **verbatim** to the user (both
lines, including the progress bar). Do not add, remove, explain, or wrap it in
anything else.
